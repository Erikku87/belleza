export interface Banner {
  id: string;
  heading: string;
  subheading: string;
  text: string;
  image: string;
}

export interface Picture {
  id: string;
  heading: string;
  subheading: string;
  text: string;
  image: string;
}
